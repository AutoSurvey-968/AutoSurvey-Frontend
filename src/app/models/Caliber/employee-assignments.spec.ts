import { EmployeeAssignments } from './employee-assignments';

describe('EmployeeAssignments', () => {
  it('should create an instance', () => {
    expect(new EmployeeAssignments()).toBeTruthy();
  });
});
