import { BatchcalendarDirective } from './batchcalendar.directive';

describe('BatchcalendarDirective', () => {
  it('should create an instance', () => {
    const directive = new BatchcalendarDirective();
    expect(directive).toBeTruthy();
  });
});
